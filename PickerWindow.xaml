<Window x:Class="ArkCommander.PickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="560" Height="680"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI"
        FontSize="14">

    <Border CornerRadius="12"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            Background="{StaticResource BackgroundGradient}">

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Background="Transparent" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <TextBlock x:Name="TitleText" FontSize="16" FontWeight="Bold"/>
            </Grid>

            <TextBox Grid.Row="1" x:Name="SearchBox" Margin="0,10"
                     TextChanged="Search_TextChanged"/>

            <Border Grid.Row="2" Background="{StaticResource PanelBrush}"
                    CornerRadius="12" Padding="6">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="RowsPanel"/>
                </ScrollViewer>
            </Border>

            <StackPanel Grid.Row="3" x:Name="QtyRow" Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Количество:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="QtyBox" Width="90" Text="1"/>
            </StackPanel>

            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="OK" Click="Ok_Click" Margin="0,0,6,0"/>
                <Button Grid.Column="1" Content="Отмена" Click="Cancel_Click" Margin="6,0,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>

