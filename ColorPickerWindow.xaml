<Window x:Class="ArkCommander.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор цвета"
        Width="320"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI"
        FontSize="13">

    <Border Background="{StaticResource PanelBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1" CornerRadius="12" Padding="16">

        <StackPanel>
            <TextBlock Text="Выбор цвета" FontWeight="SemiBold" Margin="0,0,0,10"/>

            <Border x:Name="Preview" Height="48" CornerRadius="8"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                    Margin="0,0,0,12"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="40"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="A" VerticalAlignment="Center"/>
                <Slider Grid.Column="1" x:Name="ASlider" Minimum="0" Maximum="255" ValueChanged="Slider_ValueChanged"/>
                <TextBlock Grid.Column="2" x:Name="ALabel" HorizontalAlignment="Right"/>

                <TextBlock Grid.Row="1" Text="R" VerticalAlignment="Center"/>
                <Slider Grid.Row="1" Grid.Column="1" x:Name="RSlider" Minimum="0" Maximum="255" ValueChanged="Slider_ValueChanged"/>
                <TextBlock Grid.Row="1" Grid.Column="2" x:Name="RLabel" HorizontalAlignment="Right"/>

                <TextBlock Grid.Row="2" Text="G" VerticalAlignment="Center"/>
                <Slider Grid.Row="2" Grid.Column="1" x:Name="GSlider" Minimum="0" Maximum="255" ValueChanged="Slider_ValueChanged"/>
                <TextBlock Grid.Row="2" Grid.Column="2" x:Name="GLabel" HorizontalAlignment="Right"/>

                <TextBlock Grid.Row="3" Text="B" VerticalAlignment="Center"/>
                <Slider Grid.Row="3" Grid.Column="1" x:Name="BSlider" Minimum="0" Maximum="255" ValueChanged="Slider_ValueChanged"/>
                <TextBlock Grid.Row="3" Grid.Column="2" x:Name="BLabel" HorizontalAlignment="Right"/>
            </Grid>

            <TextBox x:Name="HexBox" Margin="0,10,0,12" IsReadOnly="True"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="OK" Click="Ok_Click" Margin="0,0,6,0"/>
                <Button Grid.Column="1" Content="Отмена" Click="Cancel_Click" Margin="6,0,0,0"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>
